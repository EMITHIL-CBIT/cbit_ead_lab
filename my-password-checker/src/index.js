import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import PasswordStrengthChecker from './App';

ReactDOM.render(
  <React.StrictMode>
    <PasswordStrengthChecker />
  </React.StrictMode>,
  document.getElementById('root')
);
